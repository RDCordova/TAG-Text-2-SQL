# Core app framework
streamlit>=1.37.0

# AWS SDK for Bedrock model access
boto3>=1.34.0
botocore>=1.34.0

# LangChain ecosystem (core + community modules)
langchain-core>=0.3.0
langchain-community>=0.3.0

# PDF text extraction for RAG context building
PyPDF2>=3.0.1

# Embeddings and vector store
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4

# Optional: for structured outputs and text parsing
pydantic>=2.8.0

# Optional: database connectors for wider compatibility
sqlalchemy>=2.0.0
